# PostgreSQL connection
# NOTA: ahora requiere permisos de ESCRITURA (anular/corregir eventos auditados).
DATABASE_URL=postgresql://user:password@n8n.dalipalacios.com.ar:5432/add_sa

# Secreto para firmar la cookie de sesión del login (HMAC). Generar uno largo y aleatorio.
# Ej: openssl rand -hex 32
AUTH_SECRET=cambiar-por-un-secreto-largo-y-aleatorio

# App config
NEXT_PUBLIC_APP_NAME=ADD Tracker
NEXT_PUBLIC_APP_URL=http://localhost:3000
